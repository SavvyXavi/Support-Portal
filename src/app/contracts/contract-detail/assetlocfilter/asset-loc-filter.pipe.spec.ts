import { AssetLocFilterPipe } from './asset-loc-filter.pipe';

describe('AssetLocFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetLocFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
